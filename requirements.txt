# Core LLM / agent framework
langchain-core==1.4.9
langchain-groq==1.1.3
langgraph==1.2.9

# Document ingestion
unstructured[md]==0.24.1

# Vector store + embeddings (local embeddings, no external API key needed)
# Note: langchain-huggingface is deliberately NOT used here -- as of this
# writing it caps at langchain-core<1.0.0, which conflicts with
# langchain-groq/langgraph's langchain-core>=1.4 requirement. We call
# sentence-transformers directly via a small custom Embeddings class
# instead (see app/vectorstore.py::LocalSentenceTransformerEmbeddings).
langchain-chroma>=0.2.0,<1.0.0
chromadb>=1.0.9,<2.0.0
sentence-transformers>=3.0.0,<4.0.0

# Config / utils
pydantic>=2.13,<3.0
pydantic-settings==2.14.2
python-dotenv==1.2.2

# Optional HTTP API
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# Tests
pytest==9.1.1
